# Continuity Handoff - 2026-07-15

## Current status

The security and modernization audit was executed through the live Supabase project. The requested pending items were applied and verified.

## Work log — 2026-09-08

- Checkout provisório de PIX implementado com pedido registrado antes do pagamento, 10% de desconto adicional calculado no banco, QR Code EMV, código copia e cola e cópia direta da chave PIX.
- Chave PIX pública configurada na Vercel para Production e Preview.
- Checkout de cartão implementado via WhatsApp, com até 6x sem juros e parcela mínima de R$ 100,00.
- PIX e cartão abrem o WhatsApp com mensagem completa baseada nos dados confirmados pelo servidor: pedido, itens, endereço, CEP, região, prazo, forma de pagamento, frete, descontos e total. Como o fluxo usa `wa.me`, o cliente confirma o envio; há botão de fallback se o navegador bloquear a abertura automática.
- Migration `20260908190000_add_pix_checkout_discount.sql` aplicada ao Supabase remoto. A verificação controlada confirmou retirada 10% + PIX 10%, removeu o pedido temporário e restaurou o estoque.
- Hotfix `20260908213000_fix_checkout_wrapper_privileges.sql` aplicado após PIX e cartão falharem com PostgreSQL `42501`: o wrapper público do checkout agora é `SECURITY DEFINER` com `search_path = ''`, executável somente por `authenticated`, enquanto a implementação `app_private` continua inacessível diretamente. Duas verificações remotas sob o papel autenticado passaram e limparam os pedidos temporários.
- Gateway Pagar.me/Stone continua deliberadamente desativado enquanto o fluxo provisório estiver vigente.
- Deploy de produção `dpl_5pWeCw1ZaG5RDrvE9dqjMXPDwHzq` concluído com status READY e alias `https://allvino-app.vercel.app` atualizado.
- Verificações: typecheck, security check, build, 35 feature guards, regras de domínio, 54 testes e checkout RPC remoto aprovados; o pedido controlado foi removido e o estoque restaurado. Interface de PIX/cartão validada em produção sem falhas visíveis.

### Próxima retomada

- Repetir no navegador um pedido real controlado de PIX e outro de cartão após o hotfix, confirmando recebimento da mensagem e leitura do QR Code pelo aplicativo bancário.
- Quando o gateway definitivo for escolhido, substituir o fluxo manual preservando o contrato de pedido e os dados logísticos atuais.

## Work log — 2026-09-07

- Banners de promoção: corrigido o salvamento atômico das imagens desktop/mobile, campanha, desconto e produtos vinculados no Supabase.
- Catálogo público: banners responsivos passaram a ser carregados da fonte central, com clique para a seleção de produtos em promoção e tags/preços promocionais.
- Painel admin: removida a funcionalidade de Tema dos banners; a arte enviada pelo administrador é exibida sem sobreposição de tema. Adicionado controle de cor hexadecimal para o texto do botão.
- Produção: migration remota aplicada, schema validado, build/typecheck/testes executados e deploy Vercel concluído com status READY.
- Catálogo admin: o erro ao cadastrar o produto foi identificado como SKU Sankhya duplicado (`7814`). Nenhuma alteração de código foi feita neste ponto, conforme orientação; o SKU deve ser único ou o produto existente deve ser editado.

### Próxima retomada

- Se for necessário cadastrar o novo vinho, informar um SKU Sankhya ainda não utilizado e repetir o cadastro.
- Preservar a campanha ativa e os vínculos atuais ao fazer novos ajustes no catálogo.

## Completed

- Applied Supabase migrations through `20260715112805_harden_legacy_analytics_and_extensions`.
- Restricted unpublished product visibility and removed permissive legacy RLS policies.
- Hardened legacy functions, wrappers, view security, storage listing, and analytics writes.
- Executed `npm run verify:checkout-rpc` against the linked Supabase project multiple times. It passed and cleaned up the test order.
- `npm run smoke` passed with 35 feature guards.
- `npm run typecheck` passed.
- Fixed the encoding issues detected by smoke checks in the API routes.

## Deferred before production

- Supabase Auth leaked-password protection is still disabled because the project is on the Free plan. Before production, upgrade the organization/project to Pro or above and enable it under Authentication > Attack Protection > Prevent use of leaked passwords.
- Review whether legacy analytics should remain disabled or be replaced by a controlled server-side analytics path.
- Decide whether to address any remaining Supabase infrastructure warnings before production launch.

## Important validation result

The live checkout test confirmed server-side subtotal, discount, total, payment defaults, and stock reservation. The test used product `Arkos Branco` and automatically restored stock and deleted the test order.

## Resume instruction

Before production release, continue with the deferred Auth/security configuration review. Then run migration list, Security Advisor, checkout RPC verification, smoke, typecheck, tests, and production build again.
