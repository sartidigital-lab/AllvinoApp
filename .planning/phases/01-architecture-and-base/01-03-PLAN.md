---
wave: 3
depends_on: ["01-01"]
files_modified: [".github/workflows/ci.yml"]
autonomous: true
---

# Plan 01-03: CI/CD Pipeline

## Goal
Automate code quality checks via GitHub Actions.

## Requirements
- ARCH-04

## Tasks

```xml
<task id="01-03-01">
  <title>Setup GitHub Actions CI Workflow</title>
  <read_first>
    - package.json
  </read_first>
  <action>
    Create `.github/workflows/ci.yml`.
    Define a workflow that triggers on pull_request to `main`.
    Add steps to checkout code, setup Node.js, run `npm install`, and run `npm run lint` and `npm run build`.
    Ensure `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` are provided as dummy env vars in the build step so Zod doesn't fail the build in CI.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists.
    - Workflow file contains `npm run lint` and `npm run build` commands.
  </acceptance_criteria>
</task>
```
