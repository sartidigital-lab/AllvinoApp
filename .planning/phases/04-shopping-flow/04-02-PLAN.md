---
wave: 2
depends_on: ["04-01-PLAN.md"]
files_modified:
  - "src/types/database.ts"
  - "src/lib/database/orders.ts"
  - "src/app/checkout/page.tsx"
  - "src/app/conta/page.tsx"
autonomous: true
---

# Plan 04-02: Fluxo de Checkout e Histórico de Compras

## Context & Objective
Implement the checkout process where the user finalizes the purchase (simulating payment) and the order history section where they can view past purchases. This requires setting up an `orders` structure in the database layer.

## Tasks

```xml
<task>
  <id>1</id>
  <title>Define Order Database Types</title>
  <description>Extend the database types to include Orders and Order Items.</description>
  <read_first>
    <file>src/types/database.ts</file>
  </read_first>
  <action>
    Update `src/types/database.ts` to include:
    ```typescript
    export type Order = {
      id: string;
      user_id: string;
      status: string;
      total_amount: number;
      created_at: string;
      delivery_method: string;
    };

    export type OrderItem = {
      id: string;
      order_id: string;
      wine_id: string;
      quantity: number;
      price_at_time: number;
    };
    ```
  </action>
  <acceptance_criteria>
    <criterion>`src/types/database.ts` exports `Order` and `OrderItem` types.</criterion>
  </acceptance_criteria>
</task>

<task>
  <id>2</id>
  <title>Create Order Supabase Layer</title>
  <description>Create functions to save a new order and fetch order history for a user.</description>
  <read_first>
    <file>src/lib/database/wines.ts</file>
    <file>src/types/database.ts</file>
  </read_first>
  <action>
    Create `src/lib/database/orders.ts`. Import Supabase SSR client.
    Export `createOrder(userId: string, cartItems: any[], total: number, method: string)` which inserts into `orders` and then inserts into `order_items`.
    Export `getUserOrders(userId: string)` which fetches orders for a user.
  </action>
  <acceptance_criteria>
    <criterion>`src/lib/database/orders.ts` exports `createOrder` and `getUserOrders`.</criterion>
  </acceptance_criteria>
</task>

<task>
  <id>3</id>
  <title>Build Checkout Page</title>
  <description>Build the Checkout UI according to the reference and integrate with order creation.</description>
  <read_first>
    <file>checkout.html</file>
    <file>src/context/CartContext.tsx</file>
  </read_first>
  <action>
    Create `src/app/checkout/page.tsx` (Client Component).
    Use `useCart` to list items and get the total.
    Include form fields for delivery address or pickup.
    Include a "Finalizar Compra" button that triggers `createOrder` (using the currently authenticated user's ID via Supabase client `getUser()`).
    After successful creation, call `clearCart()` and redirect to `/conta` or a success page.
  </action>
  <acceptance_criteria>
    <criterion>`src/app/checkout/page.tsx` exists and uses `useCart`.</criterion>
    <criterion>It simulates order creation and clears the cart on success.</criterion>
  </acceptance_criteria>
</task>

<task>
  <id>4</id>
  <title>Build Account/Order History Page</title>
  <description>Implement the user account page displaying their order history.</description>
  <read_first>
    <file>conta.html</file>
    <file>src/lib/database/orders.ts</file>
  </read_first>
  <action>
    Create `src/app/conta/page.tsx` (can be Server Component or Client Component fetching data).
    Ensure route is protected (already handled by middleware in Phase 2).
    Fetch user orders using `getUserOrders`.
    Render the order history UI based on `conta.html` structure.
  </action>
  <acceptance_criteria>
    <criterion>`src/app/conta/page.tsx` exists and fetches/displays orders.</criterion>
  </acceptance_criteria>
</task>
```

## Verification

<verification_criteria>
- Going to `/checkout` displays the cart items correctly.
- Completing checkout empties the cart and redirects to `/conta`.
- `/conta` correctly displays the placed order.
</verification_criteria>

<must_haves>
- SHOP-03
- SHOP-04
</must_haves>
