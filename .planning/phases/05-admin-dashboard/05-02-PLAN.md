---
wave: 2
depends_on: ["05-01-PLAN.md"]
files_modified:
  - "src/app/admin/catalogo/page.tsx"
  - "src/app/admin/promocoes/page.tsx"
  - "src/app/admin/equipe/page.tsx"
  - "src/lib/database/wines.ts"
autonomous: true
---

# Plan 05-02: Catalog & Content Management

## Goal
Implement the CRUD interfaces for the Catalog, Promotions, and Team members, matching the design of `admin.html`.

## Tasks

1. **Admin Catalog Page (`src/app/admin/catalogo/page.tsx`)**
   - `<read_first>`: `admin.html`, `src/lib/database/wines.ts`
   - `<action>`: Convert the `#tab-catalogo` section. Fetch wines using `getWines()`. Render them in the table. Implement a modal/form (using Tailwind dialog or custom modal state) to add/edit wines. Implement photo upload to Supabase Storage bucket (`produtos`). Update `src/lib/database/wines.ts` with `createWine`, `updateWine`, `deleteWine` if missing.
   - `<acceptance_criteria>`: Page renders a table of wines with edit/delete actions. Form handles Supabase storage upload.

2. **Admin Promotions Page (`src/app/admin/promocoes/page.tsx`)**
   - `<read_first>`: `admin.html`
   - `<action>`: Convert the `#tab-promo` section. Create a basic interface to list and add promotions/banners. Require Supabase table `promocoes`. Create the CRUD functions.
   - `<acceptance_criteria>`: Page renders promotions table and Add Promotion modal.

3. **Admin Team Page (`src/app/admin/equipe/page.tsx`)**
   - `<read_first>`: `admin.html`
   - `<action>`: Convert the `#tab-usuarios` section. Create an interface to list team members.
   - `<acceptance_criteria>`: Page renders team table.
