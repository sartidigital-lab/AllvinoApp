---
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/middleware.ts
  - src/app/auth/callback/route.ts
autonomous: true
---

# Plan 02-02: Middleware e Proteção de Rotas

## Context
This plan implements the Next.js Middleware to protect routes like `/conta` and `/admin`, redirecting unauthenticated users to the Home page with the modal trigger. It also implements the OAuth callback route to establish sessions when returning from Google.

## Requirements
- AUTH-03: Redirect unauthorized accesses to `/?login=true`.
- AUTH-04: Implement OAuth callback route.

## Tasks

```xml
<task id="02-02-01" autonomous="true">
  <action>
    Create the Middleware for route protection.
    Create `src/middleware.ts`.
    Import `createServerClient` from `@supabase/ssr` to refresh the session and check auth state.
    Match routes: `/conta/:path*` and `/admin/:path*`.
    If no valid session is found (`data.session` is null), redirect the user to `new URL('/?login=true', request.url)`.
    Ensure `supabaseResponse.cookies.set()` logic is implemented as per Supabase SSR docs to refresh expired tokens.
  </action>
  <read_first>
    src/utils/supabase/server.ts
  </read_first>
  <acceptance_criteria>
    - `src/middleware.ts` exists and matches `/conta` and `/admin`
    - Contains logic to redirect to `/?login=true`
    - `npm run lint` passes
  </acceptance_criteria>
</task>

<task id="02-02-02" autonomous="true">
  <action>
    Implement OAuth Callback Route.
    Create `src/app/auth/callback/route.ts` as a Route Handler.
    It should extract the `code` and `next` parameters from the request URL.
    Use `createClient()` from `src/utils/supabase/server.ts`.
    If `code` is present, call `await supabase.auth.exchangeCodeForSession(code)`.
    Redirect to the `next` parameter or to `/` if not provided.
  </action>
  <read_first>
    src/utils/supabase/server.ts
  </read_first>
  <acceptance_criteria>
    - `src/app/auth/callback/route.ts` exists
    - Uses `exchangeCodeForSession`
    - `npm run lint` passes
  </acceptance_criteria>
</task>
```

## Verification
- [ ] Run `npm run lint`
- [ ] Run `npm run build`
- [ ] Test the Middleware by visiting `http://localhost:3000/conta`. Expect redirection to `http://localhost:3000/?login=true`.
