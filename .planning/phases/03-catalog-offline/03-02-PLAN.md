---
wave: 2
depends_on: ["03-01-PLAN.md"]
files_modified:
  - "src/hooks/useWines.ts"
  - "src/app/catalogo/page.tsx"
  - "src/app/catalogo/[id]/page.tsx"
autonomous: true
---

# Plan 03-02: Dinamização do Catálogo e Estratégia Offline-First

## Context & Objective
Combine the Supabase and IndexedDB layers into a unified data hook (`useWines`), and build the dynamic Catalog List and Wine Detail pages to consume this hook. This ensures the UI is offline-capable out of the box.

## Tasks

```xml
<task>
  <id>1</id>
  <title>Create Offline-First Data Hook</title>
  <description>Create a custom React hook that abstracts data fetching, attempting to fetch from Supabase and falling back to IndexedDB if offline or on network failure.</description>
  <read_first>
    <file>src/lib/database/wines.ts</file>
    <file>src/lib/offline/idb.ts</file>
  </read_first>
  <action>
    Create `src/hooks/useWines.ts`.
    Implement `useWines()` which maintains `wines` (array), `isLoading` (boolean), and `isOffline` (boolean) state.
    On mount, the hook should:
    1. Try `fetchWinesFromSupabase()`.
    2. If successful, update state and call `cacheWines(data)` to update the local cache.
    3. If it fails (network error), set `isOffline(true)`, and call `getCachedWines()` to load data from IndexedDB.
    Also implement `useWine(id: string)` with the same fallback pattern for a single wine.
  </action>
  <acceptance_criteria>
    <criterion>`src/hooks/useWines.ts` exports `useWines` and `useWine` hooks.</criterion>
    <criterion>The hook imports and uses both `fetchWinesFromSupabase` and `getCachedWines`.</criterion>
  </acceptance_criteria>
</task>

<task>
  <id>2</id>
  <title>Build Dynamic Catalog Page</title>
  <description>Create the main Catalog page, converting `catalogo.html` logic to a Next.js App Router dynamic React component.</description>
  <read_first>
    <file>catalogo.html</file>
    <file>src/hooks/useWines.ts</file>
  </read_first>
  <action>
    Create `src/app/catalogo/page.tsx` as a Client Component (`"use client"`).
    Use the `useWines` hook.
    Render a grid of wine cards. Display a loading spinner if `isLoading` is true.
    Display a visual indicator (e.g., a "Modo Offline" badge) if `isOffline` is true.
    Each wine card should link to `/catalogo/${wine.id}`.
    If the array is empty and not loading, show a "Nenhum vinho encontrado" message.
  </action>
  <acceptance_criteria>
    <criterion>`src/app/catalogo/page.tsx` exists and is a client component using `useWines`.</criterion>
    <criterion>The UI contains links matching the `/catalogo/[id]` dynamic route pattern.</criterion>
  </acceptance_criteria>
</task>

<task>
  <id>3</id>
  <title>Build Dynamic Wine Detail Page</title>
  <description>Create the individual wine detail page to display information about a specific wine.</description>
  <read_first>
    <file>src/hooks/useWines.ts</file>
  </read_first>
  <action>
    Create `src/app/catalogo/[id]/page.tsx` as a Client Component (`"use client"`).
    Extract the `id` param. Use the `useWine(id)` hook to fetch the data.
    Display wine details (name, image, description, price, stock).
    Display a loading state while fetching.
    Display a "Modo Offline" badge if `isOffline` is true.
    Include a back button to `/catalogo`.
  </action>
  <acceptance_criteria>
    <criterion>`src/app/catalogo/[id]/page.tsx` exists and uses `useWine`.</criterion>
    <criterion>The detail page handles the loading state appropriately.</criterion>
  </acceptance_criteria>
</task>
```

## Verification

<verification_criteria>
- The `/catalogo` route renders without errors.
- The `/catalogo/[id]` route renders without errors.
- The `useWines` hook is correctly coordinating between Supabase and IndexedDB.
</verification_criteria>

<must_haves>
- CATL-02
- CATL-03
- CATL-04
</must_haves>
