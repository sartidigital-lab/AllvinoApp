---
wave: 3
depends_on: ["03-02-PLAN.md"]
files_modified:
  - "src/app/layout.tsx"
  - "src/components/layout/Navbar.tsx"
autonomous: true
---

# Plan 03-03: Integração do Catálogo no Layout

## Context & Objective
Integrate the newly created catalog routes into the application's global navigation so users can discover and access them seamlessly.

## Tasks

```xml
<task>
  <id>1</id>
  <title>Create Navbar Component</title>
  <description>Extract the navigation bar into a reusable component and add links to the Catalog.</description>
  <read_first>
    <file>src/app/layout.tsx</file>
    <file>index.html</file>
  </read_first>
  <action>
    Create `src/components/layout/Navbar.tsx`.
    Include standard navigation links (Home, Catálogo `/catalogo`, etc).
    Use Next.js `Link` component.
    Add visual cues for active routes if desired.
  </action>
  <acceptance_criteria>
    <criterion>`src/components/layout/Navbar.tsx` exists and contains a `<Link href="/catalogo">` element.</criterion>
  </acceptance_criteria>
</task>

<task>
  <id>2</id>
  <title>Update Global Layout</title>
  <description>Inject the Navbar into the global layout so it is present on all pages.</description>
  <read_first>
    <file>src/app/layout.tsx</file>
    <file>src/components/layout/Navbar.tsx</file>
  </read_first>
  <action>
    Modify `src/app/layout.tsx` to import and render `<Navbar />` inside the `<body>` above the `{children}` wrapper.
  </action>
  <acceptance_criteria>
    <criterion>`src/app/layout.tsx` imports and renders `Navbar`.</criterion>
  </acceptance_criteria>
</task>
```

## Verification

<verification_criteria>
- The Navbar appears globally on all pages.
- Clicking the Catalog link correctly navigates to `/catalogo`.
</verification_criteria>

<must_haves>
- CATL-02
</must_haves>
